export enum Gender {
  Male = 'Male',
  Female = 'Female',
}

export enum UserType {
  Admin = 'Admin',
  User = 'User',
}

export enum SupportedLocales {
  am = 'am',
  en = 'en',
  or = 'or',
  ti = 'ti',
}

export enum Status {
  Enabled = 'Enabled',
  Disabled = 'Disabled',
  Deleted = 'Deleted',
}

export enum EditableStatus {
  Enabled = 'Enabled',
  Disabled = 'Disabled',
}
